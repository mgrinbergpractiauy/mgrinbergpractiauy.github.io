import React, { forwardRef } from "react";
import { Avatar } from "@mui/material";

const AppHeader = () => {
 
  const user={};

  return (
    <header>
      
    </header>
  );
};

export default AppHeader;
