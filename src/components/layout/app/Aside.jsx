import AngleDoubleLeft from "Assets/icons/AngleDoubleLeft";
import Layers from "Assets/icons/Layers";
import { useSideMenu } from "Hooks/useMenu";
import React from "react";
import { NavLink } from "react-router-dom";

const Aside = ({ showSidebar, setShowSidebar }) => {
  const { menu } = useSideMenu();
  return (
    <aside>
      
    </aside>
  );
};

export default Aside;
